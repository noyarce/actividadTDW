import React, { useState } from "react";
import { Container } from "@mui/material";

const Main = () => {
  return (
    <Container fixed sx={{ height: 1 }}>
  Hola. soy la tele
    </Container>
  );
};

export default Main;
